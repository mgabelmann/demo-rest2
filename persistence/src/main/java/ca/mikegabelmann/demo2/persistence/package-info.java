/**
 * All classes necessary for interacting with one database 'domain'. Includes all JPA, Repository (DAO), Services and
 * Facades for managing multiple service interactions and business validation(s).
 */
package ca.mikegabelmann.demo2.persistence;
