/**
 * <p>Interfaces and implementations for the service layer. All logic, validation, transformation of data from
 * database is controlled here. If there are errors, exceptions should be thrown, preferably runtime, so they
 * can be handled at another layer. They are a 1:1 mapping only.</p>
 */
package ca.mikegabelmann.demo2.persistence.service;
