package ca.mikegabelmann.demo2.controller.rest;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.web.bind.annotation.RestController;

@RestController
public class GroupCodeRestController {
    /** Path for REST endpoint. */
    public static final String PATH_GROUP_TYPE_SEARCH1 = "/groupCode/search";

    /** Logger. */
    private static final Logger LOG = LoggerFactory.getLogger(GroupCodeRestController.class);


}
